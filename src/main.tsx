import React from "react";
import ReactDOM from "react-dom/client";
import Slider3D from "./html/Slider3D.tsx";

ReactDOM.createRoot(document.getElementById("app")!).render(
  <React.StrictMode>
    <Slider3D />
  </React.StrictMode>
);
